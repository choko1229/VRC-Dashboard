# デスクトップエージェントをexe化するためだけの依存関係（本体アプリのpyproject.tomlには含めない）。
# インストール:
#   .venv/Scripts/pip install -r desktop_agent/requirements-build.txt
pyinstaller>=6.10.0
pystray>=0.19.5
Pillow>=10.4.0
# OSのネイティブ証明書検証を使うため（環境によってPyInstaller同梱のOpenSSLの検証が
# 一部サイトの証明書チェーンで失敗することがある。desktop_agent/main.py参照）。
truststore>=0.9.0
